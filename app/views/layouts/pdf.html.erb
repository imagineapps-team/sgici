<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @titulo || 'Relatório' %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      font-size: 10px;
      line-height: 1.4;
      color: #333;
      background: #fff;
    }

    .container {
      padding: 20px;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 2px solid #4472C4;
    }

    .header-logo {
      font-size: 18px;
      font-weight: bold;
      color: #4472C4;
    }

    .header-info {
      text-align: right;
      font-size: 9px;
      color: #666;
    }

    /* Title */
    .title {
      text-align: center;
      margin-bottom: 20px;
    }

    .title h1 {
      font-size: 16px;
      color: #333;
      margin-bottom: 5px;
    }

    .title .subtitle {
      font-size: 10px;
      color: #666;
    }

    /* Table */
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
      font-size: 9px;
    }

    th {
      background-color: #4472C4;
      color: white;
      font-weight: bold;
      padding: 8px 6px;
      text-align: left;
      border: 1px solid #3563a8;
    }

    th.text-right {
      text-align: right;
    }

    td {
      padding: 6px;
      border: 1px solid #ddd;
      vertical-align: top;
    }

    td.text-right {
      text-align: right;
    }

    td.text-center {
      text-align: center;
    }

    tr:nth-child(even) {
      background-color: #f8f9fa;
    }

    tr:hover {
      background-color: #e8f4fd;
    }

    /* Footer row - only on last page */
    tfoot {
      display: table-row-group;
    }

    tfoot tr {
      background-color: #e2efda !important;
      font-weight: bold;
    }

    tfoot td {
      border: 1px solid #70ad47;
    }

    /* Status badges */
    .badge {
      display: inline-block;
      padding: 2px 6px;
      border-radius: 3px;
      font-size: 8px;
      font-weight: bold;
    }

    .badge-recebido {
      background-color: #fff3cd;
      color: #856404;
    }

    .badge-transmitido {
      background-color: #cce5ff;
      color: #004085;
    }

    .badge-processado {
      background-color: #d4edda;
      color: #155724;
    }

    .badge-cancelado {
      background-color: #f8d7da;
      color: #721c24;
    }

    /* Footer - only at end of document */
    .footer {
      margin-top: 30px;
      padding: 10px 0;
      font-size: 8px;
      color: #666;
      border-top: 1px solid #ddd;
      display: flex;
      justify-content: space-between;
    }

    /* Page break */
    .page-break {
      page-break-before: always;
    }

    /* No data message */
    .no-data {
      text-align: center;
      padding: 40px;
      color: #666;
      font-style: italic;
    }

    /* Summary box */
    .summary {
      background-color: #f8f9fa;
      border: 1px solid #ddd;
      border-radius: 4px;
      padding: 15px;
      margin-bottom: 20px;
    }

    .summary-title {
      font-weight: bold;
      color: #4472C4;
      margin-bottom: 10px;
    }

    .summary-grid {
      display: flex;
      gap: 20px;
    }

    .summary-item {
      flex: 1;
    }

    .summary-label {
      font-size: 8px;
      color: #666;
      text-transform: uppercase;
    }

    .summary-value {
      font-size: 14px;
      font-weight: bold;
      color: #333;
    }

    @media print {
      body {
        print-color-adjust: exact;
        -webkit-print-color-adjust: exact;
      }

      /* Prevent tfoot from repeating on each page */
      tfoot {
        display: table-row-group;
      }

      /* Prevent thead from repeating on each page */
      thead {
        display: table-row-group;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <%= yield %>
  </div>
</body>
</html>
